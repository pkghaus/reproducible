{
  "name": "pkghaus-reproducible",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "ls test/*.test.js >/dev/null && node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "4.131.1"
  }
}
